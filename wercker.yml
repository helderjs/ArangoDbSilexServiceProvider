box: wercker/php
services:
    - helderjs/arangodb
build:
    steps:
        - script:
            name: configuring database
            code: arangosh --javascript.execute arangodb.js
        - script:
            name: install dependencies
            code: composer install --no-interaction --prefer-source
        - script:
            name: run unit tests
            code: phpunit