box: wercker/php
services:
    - id: arangodb:2.8.7
      env: ARANGO_ROOT_PASSWORD=pass2arango
build:
    steps:
        - script:
            name: install dependencies
            code: composer install --no-interaction --prefer-source
        - script:
            name: run unit tests
            code: phpunit